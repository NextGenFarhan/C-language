int a; // declare an integer variable 'a'.
float b; //declare a float variiable 'b'.
int a=10; //declare and initialize 'a' with 10.

%d is called as format specifier.

MODULUS OPERATOR IS USED TO FIND THE REMAINDER.
{PRINTF("THE REMAINDER WHEN a IS DIVIDED BY b IS :%d", a%b)}

In c language '=' is assignment operator and '==' is equal to and != is not equal t0.

{<,>,,=,.=,==,!=}are realtional operators.